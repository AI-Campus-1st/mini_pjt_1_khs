# 미니프로젝트 가이드 — 공공데이터 기반 지역별 통계 분석

> **핵심 기술:** Python · NumPy · Pandas · SQL  
> **데이터 출처:** 공공데이터포털(data.go.kr) 및 연계 포털  
> **최종 산출물:** 분석 리포트 + Github Repository

---

## 1. 프로젝트 개요

본 미니프로젝트는 직접 가설을 수립하고 공공데이터를 탐색·연결하여 논리적인 지역 통계 인사이트를 도출하는 경험을 제공하기 위해 설계되었습니다.

### 핵심 목표

- 문제 정의 및 가설 수립: "어떤 분석을 수행할 것인가?", "어떤 인사이트를 도출할 것인가?"를 스스로 고민
- 데이터 파이프라인 경험: 공공데이터(CSV/JSON) 수집 → RDBMS/SQL 데이터베이스 구축 → SQL & Pandas 기반 데이터 정제 및 통합 분석
- 본인의 논리적 언어로 해석: 분석 결과를 단순히 자동 요약하는 것이 아닌, 데이터 근거를 바탕으로 직접 도메인 결론 도출

> ※ 분석 결과가 "예상대로였다", "유의미한 차이가 없었다"로 끝나도 괜찮습니다. 어떻게 그 결론에 도달했는지를 설명되면 됩니다. 결과의 화려함보다 논리의 정합성이 훨씬 중요합니다.

### 학습 목표
| 영역 | 이 과제에서 훈련되는 것 |
|---|---|
| **분석 설계** | 데이터를 보고 질문을 만들고, 검증 가능한 가설로 좁히는 능력 |
| **Python / Pandas** | 실제 공공데이터의 결측·이상값·인코딩 문제를 다루는 전처리 |
| **NumPy** | 벡터 연산 기반 파생지표 계산 (비율, 밀도, 정규화, 거리) |
| **SQL** | 스키마 설계, CSV → DB 적재, JOIN·집계 기반 분석 |
| **데이터 결합** | 서로 다른 출처의 데이터를 공통 키로 연결 |
| **해석·전달** | 수치를 근거로 자기 언어의 결론을 만드는 능력 |

---

## 2. 프로젝트 수행 단계 및 로드맵


### 2일 타임라인 (권장)

#### Day 1

| 시간 | 할 일 | 체크포인트 |
|---|---|---|
| 오전 | Base 데이터 탐색 및 선정, 컬럼·분포 확인 | 데이터를 실제로 열어봤는가 |
| | 핵심 방향 + 가설 3~5개 확정 | **가설을 문장으로 적었는가** |
| 오후 | 연결 데이터 선정, 조인 키 확인 | 키가 실제로 붙는지 샘플로 검증 |
| | 전처리 + 스키마 설계 + DB 적재 | **Day 1 종료 시 DB에 데이터가 들어가 있을 것** |

> 🚩 Day 1 마감 시점에 DB 적재가 안 끝났다면, 데이터 범위를 줄이세요. (전국 → 1개 시도)

#### Day 2

| 시간 | 할 일 | 체크포인트 |
|---|---|---|
| 오전 | SQL 분석 쿼리 작성, Pandas/NumPy 파생지표 계산 | 가설별로 결과가 나왔는가 |
| | 결과 해석 정리 (본인 언어로 먼저 메모) | 해석을 먼저 쓰고 문서화는 나중에 |
| 오후 | 리포트 작성 (초안 생성 → **본인 검토**) | 논리 흐름에 구멍이 없는가 |
| 여유 시 | 심화 확장 | 기본 목표 완료 후 |


### Step 1. 분석 설계 (반나절)

1. **Base 데이터를 하나 고른다.** (4챕터 참고) 분석의 기준이 되는 축입니다.
2. **데이터를 실제로 열어본다.** 컬럼 목록, 행 수, 값의 분포, 결측 비율을 먼저 확인합니다. 설계는 데이터를 본 다음에 합니다.
3. **주 분석 방향(Main Direction) 1개를 정한다.** 한 문장으로 씁니다.
4. **하위 가설 3~5개를 만든다.** 각 가설은 *"어떤 데이터의 어떤 컬럼으로 어떻게 확인할 수 있는가"* 까지 적혀야 합니다.
5. **연결할 데이터를 고른다.** 가설을 검증하기 위해 추가적으로 필요한 데이터를 확인합니다.


**주 분석 방향을 정하기 위한 예시**

| 틀 | 형태 | 예시 형태 (주제는 직접 정할 것) |
|---|---|---|
| 분포 | X는 어떻게 퍼져 있는가 | 지역별로 A가 몰려 있는 곳/없는 곳은 어디인가 |
| 비교 | 집단 간 차이가 있는가 | 도시 지역과 비도시 지역에서 A의 구성이 다른가 |
| 관계 | X와 Y는 같이 움직이는가 | 인구 규모와 A의 수는 비례하는가 |
| 정규화 | 절대량이 아니라 밀도로 보면 | 인구 1만 명당 / 면적 1km²당으로 보면 순위가 뒤집히는가 |
| 이상치 | 예상에서 벗어난 곳은 | 회귀선에서 크게 벗어난 지자체는 어디이고 왜 그런가 |
| 시간 | 추세가 있는가 | 최근 N년간 A의 증감 방향이 지역마다 다른가 |

### Step 2. 데이터 확보

- **Base 1개 + 연결 데이터 1개**
- CSV 파일 다운로드 방식이 기본. Open API 사용은 자유이나 **필수 아님**
- 원본 파일은 **절대 직접 수정하지 않습니다.**

### Step 3. CSV의 DB화 (이 과제의 기술적 핵심)

**요구사항**

- [ ] 테이블 스키마를 **직접 설계**한다 (`df.to_sql(...)` 사용 금지)
  - 컬럼별 적절한 타입 지정 (`INTEGER`, `REAL`, `TEXT`, `DATE`)
  - PK 지정, 조인에 쓰이는 컬럼에 인덱스 생성
  - CSV 원본 컬럼명을 그대로 쓰지 말고 스네이크케이스 등으로 정규화
- [ ] 전처리 후 적재한다 (결측 처리 기준, 타입 변환, 중복 제거를 코드로 남길 것)
- [ ] 적재 검증: 원본 행 수 vs 적재 행 수 비교, NULL 비율 확인, 샘플 조회

**권장 스키마 형태 (예시)**

```sql
-- 지역 마스터 (조인 허브)
CREATE TABLE region (
    region_code   TEXT PRIMARY KEY,   -- 행정동/법정동 코드 (자릿수 통일 필수)
    sido          TEXT NOT NULL,
    sigungu       TEXT,
    dong          TEXT,
    area_km2      REAL
);

-- 사실 테이블 (Base 데이터)
CREATE TABLE store (
    store_id      TEXT PRIMARY KEY,
    name          TEXT,
    category_l    TEXT,
    category_m    TEXT,
    region_code   TEXT REFERENCES region(region_code),
    lon           REAL,
    lat           REAL
);
CREATE INDEX idx_store_region   ON store(region_code);
CREATE INDEX idx_store_category ON store(category_l, category_m);

-- 연결 데이터 (인구 등)
CREATE TABLE population (
    region_code   TEXT REFERENCES region(region_code),
    base_ym       TEXT,               -- 기준연월 'YYYY-MM'
    total_pop     INTEGER,
    household     INTEGER,
    PRIMARY KEY (region_code, base_ym)
);
```

### Step 4. 분석

- **SQL로 최소 5개 이상의 의미 있는 쿼리**를 작성합니다.
  - 단순 `SELECT *` 는 제외. `JOIN`, `GROUP BY`, 집계함수, 서브쿼리 또는 CTE(`WITH`), 조건부 집계(`CASE WHEN`) 중 최소 3종류가 등장할 것
  - 각 쿼리 위에 **"이 쿼리는 어떤 가설을 검증하는가"** 를 주석으로 남길 것
- **Pandas / NumPy로 파생지표를 계산**합니다.
  - 절대량은 대부분 인구가 많은 곳이 1등입니다. **정규화 지표를 활용하세요.** (인구 1만 명당, 면적당, 비중, 증감률, z-score 등)
  - NumPy는 벡터 연산 시 활용: `np.where`, `np.log1p`, `np.percentile`, 표준화, 상관계수 등

### Step 5. 해석과 리포트

- 각 분석 결과마다 **"수치 → 해석 → 인사이트/한계"** 세 줄을 채웁니다.
- **상관과 인과를 구분해서 쓰세요.**
- 데이터의 한계(기준시점 불일치, 결측, 커버리지 편향 등)는 없는지 확인하고 기록해두세요.

---

## 3. 심화 확장 (선택 — 기본 목표 완료 후에만)

> 기본 목표를 달성했다면, 추가로 심화 확장을 진행할 수 있습니다.

### 확장 A. 시각화

- matplotlib / seaborn 기본, 지도 시각화는 folium
- 시각화 시 **"이 차트로 무엇을 말하려는지"** 주석을 달 것. 예쁜 차트보다 목적이 분명한 차트

### 확장 B. 위치 검색 쿼리 최적화

"특정 좌표에서 반경 N km 내 시설 조회" 같은 쿼리를 만들고 성능을 개선해봅니다.

1. 나이브 구현: 전 행에 대해 거리 계산 후 필터 → 실행시간 측정
2. **바운딩 박스 선필터**: 위경도 범위로 먼저 후보를 줄이고 정밀 거리 계산
3. **인덱스 적용**: `CREATE INDEX idx_geo ON store(lat, lon);` 전후 비교
4. **개선 전/후 실행시간을 표로 비교하고, 왜 빨라졌는지 확인**

---

## 4. 데이터 제안

> 아래는 **후보 목록**입니다. 그대로 쓸 필요 없고, 직접 찾은 데이터가 있다면 더 좋습니다.  
> 데이터명으로 data.go.kr 에서 검색하면 최신 버전을 찾을 수 있습니다. (파일명 뒤 날짜는 자주 바뀝니다)

### 4-1. Base 데이터 후보

Base는 **행이 많고, 지역 정보(주소 또는 위경도)를 가지고 있고, 분류 축이 여러 개인 데이터**가 좋습니다.

#### ① 소상공인시장진흥공단 상가(상권)정보

- 링크: https://www.data.go.kr/data/15083033/fileData.do
- 내용: 영업 중인 전국 상가업소 — 상호명, 업종 대/중/소분류, 지번·도로명 주소, 행정동코드, 경도, 위도
- 규모: 수백만 행 (시도 단위로 파일 분리 제공 → **1~2개 시도만 골라 시작하는 것을 권장**)
- 왜 좋은가: 업종 계층 구조 + 위경도 + 행정동코드를 모두 갖춰, 분류별·지역별·공간적 분석이 전부 가능

#### ② 전국 OO 표준데이터 계열 (생활 인프라)

- 예: `전국도시공원정보표준데이터` (https://www.data.go.kr/data/15012890/standard.do), `전국주차장정보표준데이터`, `전국공공시설개방정보표준데이터`, `전국무인교통단속카메라표준데이터`, `전국문화축제표준데이터` 등
- 특징: 전국 지자체가 **동일 서식**으로 제출한 데이터라 컬럼이 정돈되어 있고 위경도 포함 → 입문 난이도 낮음
- 왜 좋은가: 인프라 공급량 vs 수요(인구) 구조를 보기 좋음. 다만 단독으로는 컬럼이 단조로우니 연결 데이터를 잘 선택해야 좋은 분석이 가능합니다.

---

### 4-2. 연결(Join) 데이터 후보

| 데이터 | Join Key | 분석 방향 |
|---|---|---|
| **행정안전부 지역별(행정동) 성별·연령별 주민등록 인구수**<br>https://www.data.go.kr/data/15097972/fileData.do | 행정동코드 / 지역명 | 인구 대비 밀도, 연령 구조별 수요 차이 |
| **행정안전부 지역별 연령별 주민등록 인구현황**<br>https://www.data.go.kr/data/3033304/fileData.do | 시도·시군구 | 고령화율 등 인구 구조 지표 산출 |
| **행정안전부 지역별 주민등록 세대수 현황**<br>https://www.data.go.kr/data/3033295/fileData.do | 시도·시군구 | 1인세대 비중, 세대당 인구 |
| 국토교통부 아파트 매매 실거래가 | 법정동코드 | 지역 경제력·주거 특성과의 관계 |
| 대중교통 정류장/역사 정보 | 위경도 | 교통 접근성과 시설 분포의 공간적 관계 |
| 도로교통공단 교통사고 정보 | 위경도 / 시군구 | 위험 지점과 인프라 배치 |
| 소방청 화재/구급 출동 현황 | 시군구 | 대응 인프라 vs 사건 발생 |
| 한국관광공사 관광지·방문객 통계 | 시군구 | 유동인구 성격이 다른 지역의 구별 |
| 기상청 지역별 기상 관측 자료 | 관측지점 → 시군구 매핑 | 계절·기후 요인 통제 |
| 통계청 사업체/종사자 수, 지역별 소득 | 시군구 | 경제 규모 대비 정규화 |
| 행정구역 면적 (행정구역별 면적 통계) | 시군구 | 면적당 밀도 지표 |

> **※ 유의사항**  
> 데이터를 고를 때에는 연결되는 부분을 먼저 확인하세요.  
> 연결할 키가 다르거나 없다면, 연결하여 분석할 수 없습니다.   
> 만약, 시군구 단위 데이터가 있는데 연결 데이터는 주소라면, 주소를 시군구로 파싱하여  붙이는것도 방법이 될 수 있습니다.

---

### 4-3. 데이터 처리 시 발생할만한 오류 ⚠️

1. **인코딩** — 공공데이터 CSV는 `CP949`/`EUC-KR`인 경우가 많습니다.
   `pd.read_csv(path, encoding='cp949')` 또는 `encoding='utf-8-sig'` 를 시도하세요.
2. **행정동 코드 vs 법정동 코드** — **다른 체계입니다.** 이름이 같아도 코드가 다릅니다. 어느 쪽인지 확인하고 통일하세요.
3. **코드 자릿수** — 5자리(시군구) / 8자리 / 10자리가 섞여 있습니다. 조인 전에 `str[:5]` 같은 방식으로 레벨을 맞추세요.
4. **행정구역 개편** — 시군구 통합·명칭 변경으로 기준연도에 따라 코드가 다를 수 있습니다.
5. **기준시점 불일치** — 상권 데이터는 2026년, 인구는 2025년일 수 있습니다. 맞출 수 없다면 **리포트에 한계로 명시**하세요.
6. **좌표계** — 일부 데이터는 위경도(WGS84)가 아닌 TM 좌표계입니다. 그대로 쓰면 지도에 엉뚱한 곳이 찍힙니다.

---

## 5. AI 활용 가이드

AI 사용을 금지하지 않습니다. 다만 **"어디에 쓰는가"** 에 따라 학습 효과가 갈립니다.  
이 과제의 원칙은 하나입니다.

> **분석의 방향과 결과 해석은 본인의 생각이어야 하고, AI는 그 생각을 확장하거나 표현을 돕는 데 쓴다.**

### 5-1. 권장 ✅

| 용도 | 설명 |
|---|---|
| **도메인 지식 확장** | 특정 분야의 배경지식이 부족할 때 AI에게 물어 이해를 넓히는 것 |
| **가설 도출 보조** | 내가 정한 주 방향에 대해 "이런 관점도 있다"를 얻는 것 |
| **리포트·PPT 초안 작성** | 본인이 정리한 내용을 바탕으로 구조화·문장 작성 |

- **주 분석 방향(Main Question)은 본인의 아이디어여야 합니다.**
- 가설 도출 보조 단계에서 **Python 코드를 생성하지는 마세요.** 이 단계는 생각을 넓히는 단계이지 구현 단계가 아닙니다.
- 초안 생성은 권장하지만, **생성 후 검토하는 과정이 본질입니다.** 논리 전개에 오류가 있는지를 본인이 잡아내야 합니다.

> **AI 사용 기록**: 어떤 단계에서 어떻게 AI를 활용했는지 3~5줄로 남겨두세요. AI를 더 잘 활용하기 위한 지식이 됩니다.

### 5-2. 비권장 ❌

| 용도 | 이유 |
|---|---|
| **분석 결과에 대한 생성형 AI 요약** | 결과를 본인의 표현으로 해석하는 과정에 학습 포인트가 있습니다. 그 훈련이 되어 있는 상태에서 쓰는 것과, 되어 있지 않은 상태에서 쓰는 것은 결과가 같아 보여도 남는 것이 완전히 다릅니다. 지금 시점에서는 독이 됩니다. |
| 주 분석 방향 자체를 AI에게 위임 | 문제 정의 능력이 이 과제의 핵심 평가 대상 |
| 코드 전체를 생성해 붙여넣기 | 설명하지 못하는 코드는 제출물로 인정되지 않습니다 |


---

## 6. 제출물

### 6-1. 저장소 구조 (권장)

```
project/
├── README.md                # 프로젝트 개요, 실행 방법, 데이터 출처
├── data/
│   ├── raw/                 # 원본 CSV (수정 금지) (.gitignore로 제외할 것)
│   └── README.md            # 데이터 출처·기준일자·라이선스
├── db/
│   └── schema.sql           # 테이블 정의
├── src/  또는 notebooks/
│   └── analysis.ipynb       # Pandas/NumPy 기반 통계 분석 및 가설 검증 과정이 담긴 코드
└── report/
    └── report.md            # .pdf 또는 .pptx도 가능, 문제 정의, 가설, DB 구조, 분석 결과, 본인의 논리적 해석 및 시사점 포함
```

### 6-2. 리포트 목차

1. **분석 배경과 핵심 방향** — 왜 이 주제인가, 무엇을 알고 싶었는가
2. **데이터 소개** — 사용 데이터, 출처, 기준시점, 규모, 선정 이유
3. **가설** — 3~5개, 각각 "무엇으로 어떻게 검증할 것인가"까지
4. **데이터 처리 과정** — 스키마 설계 근거, 전처리 판단(결측·이상치를 어떻게 왜 처리했는지), 조인 키 설계
5. **분석 결과** — 가설별로 결과와 근거 수치 (SQL/코드 포함)
6. **해석과 인사이트** — 본인의 언어로. 예상과 달랐던 부분 포함
7. **한계와 다음 단계** — 데이터의 한계, 검증 못 한 것, 더 하고 싶은 것
8. **(선택) 심화 확장 결과** — 시각화 / 쿼리 최적화
9. **AI 활용 기록**

---

## 7. 평가 기준

| 평가 항목 | 비중 | 세부 평가 내용 |
| :--- | :---: | :--- |
| **1. 문제 정의 & 가설** | 20% | · 스스로 명확하고 타당한 가설을 수립하였는가?<br>· 데이터 연결의 개연성이 논리적인가? |
| **2. DB 구축 & SQL 활용** | 30% | · CSV 데이터를 적절한 스키마로 DB화하였는가?<br>· Complex SQL(JOIN, GROUP BY 등)을 정확하게 활용하였는가? |
| **3. 본인 언어의 인사이트** | 30% | · AI 자동 요약이 아닌 본인의 해석과 시사점이 담겨 있는가?<br>· 데이터 근거와 결론 간의 논리적 연계가 확실한가? |
| **4. 보고서 논리성** | 20% | · 보고서 초안 검토를 통해 논리 전개상의 오류를 교정했는가?<br>· 가독성 및 전달력이 우수한가? |
| *(가산)* **심화 확장** | +10% | · Visual 시각화(Folium/Plotly 등) 또는 SQL Query 최적화/인덱싱 수행 시 가산점 부여 |

**감점 요인**

- 설명하지 못하는 코드·결론
- 데이터 출처·기준일자 미기재
- 잘못된 쿼리 적용 (JOIN시 행 수 검증)
- 비교의 부정확성 (절대량 비교)

---

## 8. 제출 전 최종 체크리스트

**데이터**
- [ ] 데이터셋 3개 이상 사용 (Base 1 + 연결 2)
- [ ] 모든 데이터의 출처 URL·기준일자를 기록했다
- [ ] 조인 후 행 수 변화를 확인하고 설명할 수 있다

**DB**
- [ ] `schema.sql`을 직접 작성했다
- [ ] 조인 키에 인덱스를 걸었다
- [ ] 원본 행 수와 적재 행 수를 비교 검증했다

**분석**
- [ ] 의미 있는 SQL 쿼리 5개 이상, 각각 가설과 연결된 주석이 있다
- [ ] JOIN / GROUP BY / 서브쿼리(또는 CTE) / 조건부 집계 중 3종 이상 사용
- [ ] 정규화된 파생지표를 1개 이상 만들었다
- [ ] NumPy 벡터 연산을 실제로 사용했다

**리포트**
- [ ] 주 질문과 가설이 명시되어 있다
- [ ] 결과마다 "수치 → 해석 → 인사이트/한계"가 있다
- [ ] 상관과 인과를 구분해서 서술했다
- [ ] 예상과 달랐던 결과를 숨기지 않고 적었다
- [ ] AI 활용 기록을 남겼다
- [ ] **모든 코드와 결론을 내가 설명할 수 있다**

---

## 9. 참고 링크

- 공공데이터포털 — https://www.data.go.kr/
- 행정안전부 주민등록 인구통계 — https://jumin.mois.go.kr/
- 국가통계포털(KOSIS) — https://kosis.kr/
- 서울 열린데이터광장 — https://data.seoul.go.kr/
- 경기데이터드림 — https://data.gg.go.kr/

### 유의사항

이 과제에서 가장 흔한 후회는 **"데이터 정리하다가 시간이 다 갔다"** 입니다.  
그건 실패가 아니라 실무의 실제 비율에 가깝습니다. 다만 **범위를 줄이는 결정을 일찍 내리는 것**도 중요합니다.  
전국 데이터가 안 돌아가면 한 개 시도로, 그것도 무거우면 한 개 시군구로 줄이세요.  
**작더라도 끝까지 이어진 분석 한 줄기가, 크지만 중간에 끊긴 분석보다 훨씬 좋은 결과물입니다.**
